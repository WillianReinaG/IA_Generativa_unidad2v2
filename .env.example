# Copia este archivo como .env y pega tu clave (no subas .env a git).
OPENAI_API_KEY=

# Opcional: sobrescribe el modelo del TOML (ej. gpt-4o-mini para pruebas más baratas)
# OPENAI_MODEL=gpt-4o-mini
